const config = {
  singleQuote: true,
  printWidth: 100,
  arrowParens: 'avoid',
  trailingComma: 'all',
  jsxSingleQuote: false,
  semi: false,
  plugins: ['prettier-plugin-tailwindcss'],
}

export default config
